import 'package:flutter/material.dart';
import 'package:get/get.dart';

import 'reqister_help/reqister-help-main.dart';
class ReqisterHelp extends StatelessWidget {
  final title;
  final Widget child;
  const ReqisterHelp({Key? key, required this.title, required this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Wrap(
      crossAxisAlignment: WrapCrossAlignment.center,
      children: const [
    
      ],
    );
  }
}

import 'package:flutter/material.dart';
class GenderHelp extends StatelessWidget {
  const GenderHelp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("سلام به راهنمای جنسیت خوش آمدید"),
    );
  }
}

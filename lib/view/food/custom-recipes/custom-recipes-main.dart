import 'package:flutter/material.dart';

class CustomRecipesMain extends StatelessWidget {
  const CustomRecipesMain({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

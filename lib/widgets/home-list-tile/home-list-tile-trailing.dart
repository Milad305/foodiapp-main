import 'package:flutter/material.dart';
class HomeListTileTrailing extends StatelessWidget {
  const HomeListTileTrailing({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.arrow_forward_ios_sharp,
      size: 18,
      color: Colors.black54,
    );
  }
}
